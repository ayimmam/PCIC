{
   "name": "pcic",
   "homepage": "https://pcic.tech/",
   "private": true,
   "version": "1.0.0",
   "type": "module",
   "scripts": {
      "dev": "npm --prefix client run dev",
      "build": "npm --prefix client run build",
      "predeploy": "npm run build",
      "deploy": "gh-pages -d client/dist",
      "postinstall": "npm --prefix client install"
   },
   "devDependencies": {
      "gh-pages": "^6.3.0"
   }
}
